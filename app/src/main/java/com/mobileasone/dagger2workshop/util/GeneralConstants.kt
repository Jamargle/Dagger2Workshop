package com.mobileasone.dagger2workshop.util

interface GeneralConstants {
    companion object {
        const val CONTENT_AUTHORITY = ".my.package.name.provider"
        const val DATE_FORMAT = "yyyyMMdd_HHmmss"
    }
}
